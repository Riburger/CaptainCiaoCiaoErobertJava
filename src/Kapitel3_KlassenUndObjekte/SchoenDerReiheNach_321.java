package Kapitel3_KlassenUndObjekte;

/**
 * 3.2.1 Quiz: Schön der Reihe nach ★
 * Eine Datei ist eine Compilationseinheit, und in den allermeisten Fällen wird in einer
 * Compilationseinheit mindestens eine Typdeklaration sitzen, so etwas wie eine Klassendeklaration.
 * Optional sind dann eine package-Deklaration bzw. import-Deklarationen.
 * Eine package-Deklaration ist nur dann zwingend, wenn der Typ nicht im default
 * package deklariert wird, sondern in einem eigenen Paket. Eine import-Deklaration
 * wiederum ist rein optional und nur praktisch, denn wenn wir Typen aus anderen Paketen
 * nutzen, wollen wir uns im Allgemeinen Schreibarbeit mit vollqualifizierten
 * Typen sparen und wenig schreiben, dass nur der Typname im Quellcode auftaucht.
 * <p>
 * Wie lautet die korrekte Reihenfolge der Deklarationen?
 * 1. import-Deklaration(en), package-Deklaration, Typdeklaration(en)
 * 2. import-Deklaration(en), Typdeklaration(en), package-Deklaration
 * 3. package-Deklaration, import-Deklaration(en), Typdeklaration(en) <---------- Riburger 090823
 * 4. Typdeklaration(en), import-Deklaration(en), package-Deklaration
 * 5. Typdeklaration(en), package-Deklaration, import-Deklaration(en)
 */
public class SchoenDerReiheNach_321 {
}
